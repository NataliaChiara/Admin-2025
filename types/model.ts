export interface ProductType {
  name: string,
  slug: string;
  price: number,
  description: string,
  section: string,
  image: string,
}

export interface SectionType {
  name: string,
  slug: string,
}